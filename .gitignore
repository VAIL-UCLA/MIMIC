__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Downloaded model weights (IC-Light, YOLO) — fetched on first run.
mimic/appearance_augmentation/models/
mimic/action_augmentation/models/

# Private map from published clip UIDs back to their source scenario ids.
.uid_map.json

# Cached DepthCrafter stacks — large, and reproducible from the clips.
.depth_cache/
